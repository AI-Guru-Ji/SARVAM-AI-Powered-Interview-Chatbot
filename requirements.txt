# ── Core runtime ───────────────────────────────────────────────────
requests==2.31.0
python-dotenv==1.0.0
pydantic==2.7.4
pydantic-settings==2.3.4

# ── Web UI (presentation layer; swappable) ─────────────────────────
streamlit>=1.42.0
audio-recorder-streamlit>=0.0.10

# ── PDF rendering ──────────────────────────────────────────────────
fpdf2>=2.8.7
# WeasyPrint renders the recruiter dashboard (HTML+CSS) to a downloadable PDF.
# Needs cairo/pango system libs on Linux (libpango-1.0-0, libpangoft2-1.0-0).
weasyprint>=68.0

# ── Web UI #2: FastAPI backend for the Android mobile client ───────
# (Optional — required only when running `uvicorn ui.fastapi.app:app`.
# The Streamlit app does NOT depend on these.)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9      # multipart form parsing for audio uploads
httpx>=0.27.0                # used by Starlette TestClient AND tests
sqlalchemy>=2.0.30           # tiny session store (SQLite for MVP, Postgres ready)

# ── Testing ────────────────────────────────────────────────────────
pytest>=8.2.0
